interface:
  display_name: "Spec-driven Delivery"
  short_description: "Run one selected delivery stage manually"
  default_prompt: "Use $spec-driven-delivery to perform only the requested stage for this goal."
policy:
  allow_implicit_invocation: false
