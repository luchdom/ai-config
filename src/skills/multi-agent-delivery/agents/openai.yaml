interface:
  display_name: "Multi-Agent Delivery"
  short_description: "Route shared specialists without choosing workflow mode"
  default_prompt: "Use $multi-agent-delivery to hand this active workflow between its shared specialists without changing caller authority."
